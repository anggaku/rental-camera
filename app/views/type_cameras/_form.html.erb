<%= form_with(model: type_camera, local: true) do |form| %>
  <% if type_camera.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_camera.errors.count, "error") %> prohibited this type_camera from being saved:</h2>

      <ul>
      <% type_camera.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :type_camera_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
