<%= form_with(model: brand_product, local: true) do |form| %>
  <% if brand_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(brand_product.errors.count, "error") %> prohibited this brand_product from being saved:</h2>

      <ul>
      <% brand_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :brand_product_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
