<%= form_with(model: aksesori, local: true) do |form| %>
  <% if aksesori.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aksesori.errors.count, "error") %> prohibited this aksesori from being saved:</h2>

      <ul>
      <% aksesori.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :aksesori_name %>
  </div>

  <div class="field">
    <%= form.label :slug %>
    <%= form.text_field :slug, id: :aksesori_slug %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
